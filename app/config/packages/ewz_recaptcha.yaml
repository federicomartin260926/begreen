ewz_recaptcha:
    public_key: '%env(EWZ_RECAPTCHA_PUBLIC_KEY)%'
    private_key: '%env(EWZ_RECAPTCHA_PRIVATE_KEY)%'
    enabled: true
    # locale_key: '%kernel.default_locale%'
    locale_from_request: true   # Utilizamos esta opción para que el reCAPTCHA use el idioma del usuario
